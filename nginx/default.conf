server {
	listen 80 default_server;
	listen [::]:80 default_server;

	root /var/www/html/alersh;

	index index.html;

	server_name alersh.ru;


	location /api/ {
	    proxy_pass http://localhost:4000/;
	}


    location / {
         try_files $uri /index.html;
    }
}
